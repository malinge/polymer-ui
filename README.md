## 安装

建议使用node版本16.15.0

注意：需使用 nodejs 长期维护版本，如：[16.x]，能保证项目的稳定运行。

node16以上，安装pnpm。ts版本，打包要符合ts的编译。

```bash
# 克隆项目
git clone https://gitee.com/ailge/polymer-ui.git

# 进入项目
cd polymer-ui

# 安装依赖
pnpm install

# 运行项目
npm run dev

# 打包发布
npm run build
```
